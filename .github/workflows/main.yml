
name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: run pwd
        run: pwd
      - name: run ls -al
        run: ls -al
      - name: run test.sh
        run: chomod 777; ./test.sh
